// Wk 9 Q1 - print_diary.c
// print $HOME/.diary to stdout

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

const char *basename = ".diary";

int main(int argc, char *argv[]) {
    // Write a C program, print_diary.c, which prints the
    // contents of the file $HOME/.diary to stdout
    //The lecture example getstatus.c shows how to get the
    //value of an environment variable.

    // TODO - get the value of the HOME environment variable
    // char *home_pathname = ...

    // TODO - construct diary_pathname & calculate length
    // "$HOME/.diary"
    // home_pathname / basename

    // TODO - print the contents of the file to stdout

    return 0;
}